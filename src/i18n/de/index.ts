import type { BaseTranslation } from '../i18n-types';
import settings from './settings';

export default {
    settings,
    language: {
        flag: '🇩🇪',
        name: 'Deutsch',
    },
} satisfies BaseTranslation;
