import settings from './settings';
import type { Translation } from '../i18n-types';

export default {
    settings,
    language: {
        flag: '🇬🇧',
        name: 'English',
    },
} satisfies Translation;
